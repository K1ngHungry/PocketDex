class SetInfo:
    def __init__(self, name, code, release_date, card_count, img_url, url):
        self.name = name
        self.code = code
        self.release_date = release_date
        self.card_count = card_count
        self.img_url = img_url
        self.url = url
